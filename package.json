{
  "name": "mot-mystere",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "serverless-http": "^4.0.0"
  }
}
